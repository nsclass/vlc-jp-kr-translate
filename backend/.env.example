# Optional: DeepL API key (fallback translator)
DEEPL_API_KEY=

# Translation backend: "claude" (uses `claude -p` CLI) or "deepl"
TRANSLATION_BACKEND=claude

# Whisper model size: tiny, base, small, medium, large-v3
WHISPER_MODEL=medium

# Backend server port
BACKEND_PORT=8765
